<Window
    x:Class="RadCRHelper.Views.CountdownDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Rad CR Helper"
    Width="480"
    SizeToContent="Height"
    AllowsTransparency="True"
    Background="Transparent"
    ResizeMode="NoResize"
    ShowInTaskbar="True"
    Topmost="True"
    WindowStartupLocation="Manual"
    WindowStyle="None">

    <!--
        One dialog for every "this happens in N seconds" moment:
        Sign out, Restart, and "Keep this screen setting?".
        No close or minimize buttons by design; Alt+F4 is ignored. Esc = the safe button.
    -->
    <Border
        Margin="16"
        Padding="28,24"
        Background="{DynamicResource ApplicationBackgroundBrush}"
        BorderBrush="{DynamicResource AccentFillColorDefaultBrush}"
        BorderThickness="2"
        CornerRadius="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="24" Opacity="0.45" ShadowDepth="0" />
        </Border.Effect>

        <StackPanel>
            <TextBlock
                x:Name="TitleText"
                FontSize="24"
                FontWeight="SemiBold"
                Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                TextWrapping="Wrap" />

            <TextBlock
                x:Name="MessageText"
                Margin="0,12,0,0"
                FontSize="16"
                Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                TextWrapping="Wrap" />

            <TextBlock
                x:Name="CountdownText"
                Margin="0,20,0,6"
                FontSize="16"
                FontWeight="SemiBold"
                Foreground="{DynamicResource TextFillColorPrimaryBrush}" />

            <ProgressBar
                x:Name="CountdownBar"
                Height="6"
                Minimum="0" />

            <Grid Margin="0,24,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <ui:Button
                    x:Name="SafeButton"
                    Grid.Column="0"
                    Height="52"
                    HorizontalAlignment="Stretch"
                    Appearance="Secondary"
                    Click="SafeButton_Click"
                    FontSize="17" />

                <ui:Button
                    x:Name="ActButton"
                    Grid.Column="2"
                    Height="52"
                    HorizontalAlignment="Stretch"
                    Appearance="Primary"
                    Click="ActButton_Click"
                    FontSize="17" />
            </Grid>
        </StackPanel>
    </Border>
</Window>
