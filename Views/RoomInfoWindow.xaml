<Window
    x:Class="RadCRHelper.Views.RoomInfoWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Rad CR Helper - Room info"
    Width="560"
    Height="520"
    Background="{DynamicResource ApplicationBackgroundBrush}"
    ShowInTaskbar="True"
    Topmost="True"
    WindowStartupLocation="CenterScreen">

    <!-- For whoever is troubleshooting. Copy puts everything on the clipboard for an email or ticket. -->
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            FontSize="20"
            FontWeight="SemiBold"
            Foreground="{DynamicResource TextFillColorPrimaryBrush}"
            Text="Room info" />

        <TextBox
            x:Name="InfoText"
            Grid.Row="1"
            Margin="0,12,0,12"
            FontFamily="Consolas"
            FontSize="13"
            HorizontalScrollBarVisibility="Auto"
            IsReadOnly="True"
            TextWrapping="NoWrap"
            VerticalScrollBarVisibility="Auto" />

        <StackPanel
            Grid.Row="2"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <ui:Button
                Margin="0,0,10,0"
                Appearance="Secondary"
                Click="Refresh_Click"
                Content="Refresh" />
            <ui:Button
                Margin="0,0,10,0"
                Appearance="Secondary"
                Click="Copy_Click"
                Content="Copy" />
            <ui:Button
                Appearance="Primary"
                Click="Close_Click"
                Content="Close"
                IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
